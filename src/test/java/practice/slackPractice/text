
        //Log the following values for each size
        //.Size information .Price -Color .Stock status


        String productTitle = driver.findElement(By.id("productTitle")).getText();
        System.out.println("productTitle = " + productTitle);

        String productSize = driver.findElement(By.xpath("(//div[@class='a-row'])[7]")).getText();
        System.out.println("productSize = " + productSize);

        String productColor = driver.findElement(By.xpath("(//div[@class='a-row'])[8]")).getText();
        System.out.println("productColor = " + productColor);

        String productPrice= driver.findElement(By.xpath("(//span[@class='a-offscreen'])[1]")).getText();

    /*    String[] arr = productPrice.split("\n");
        System.out.println(Arrays.toString(arr));
        String price = arr[1];
        System.out.println(price);*/
        System.out.println("+++++++");

        System.out.println("productPrice = " + productPrice);

        String stock = TestBase.driver.findElement(By.xpath("//*[@class='a-size-medium a-color-success']")).getText();
        System.out.println("stock = " + stock);


